class Solution {
public:
    int minimumSum(int num) {
        vector<int>d;
        for(int i=0;i<4;i++){
            d.push_back(num%10);
            num/=10;
        }

        sort(d.begin(),d.end());  //从小到大排序
        return 10*(d[0]+d[1])+d[2]+d[3] ;  //贪心算法 保证最小
    }
};
